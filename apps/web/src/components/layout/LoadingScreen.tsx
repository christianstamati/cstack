/** A full-page placeholder while something the whole page needs is loading. */
export function LoadingScreen() {
  return (
    <div className="flex min-h-svh items-center justify-center text-muted-foreground text-sm">
      Loading…
    </div>
  )
}
