{
  "name": "@cstack/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The create-cstack-app CLI. Its bin and dependency are declared in the root package.json, which is what bunx installs.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
